# Coding and Validation Routine

## 목적

코딩 작업을 진행할 때 단순히 기능 구현에서 끝내지 않고, 입력 데이터와 결과 데이터의 정합성, 코드 변경의 안정성, 전체 동작의 무결성을 확인한 뒤 사용자에게 정리된 결과를 전달한다.

## 기본 원칙

1. 작업 전 현재 레포지토리 상태와 관련 파일을 확인한다.
2. 기존 코드 구조와 컨벤션을 우선 따른다.
3. 변경 범위를 명확히 정하고 관련 없는 파일은 수정하지 않는다.
4. 데이터 처리 로직이 포함된 경우 입력값, 변환 과정, 출력값을 검증한다.
5. 하드코딩은 가능한 한 피하고, 설정값, 상수, 환경 변수, 데이터 파일, 기존 설정 구조를 우선 사용한다.
6. AI가 필요하다고 판단하면 플랜, 코드 정리, 데이터 검증, 리뷰, 보조 에이전트 같은 함수나 도구를 호출해서 사용할 수 있다.
7. 기능 구현 후 테스트, 빌드, 린트 등 가능한 검증 절차를 실행한다.
8. 검증에서 문제가 발견되면 원인을 분석하고 수정한 뒤 다시 검증한다.
9. 문제가 없을 때만 변경 파일, 검증 결과, 남은 주의사항을 정리해서 보고한다.

## 프로젝트 필수 조건

1. 앱은 반드시 웹앱으로 개발한다.
2. AI 기능은 Copilot SDK 사용을 전제로 설계하고 구현한다.
3. 배포 대상은 Azure 플랫폼으로 한다.
4. 로컬 실행, 빌드 검증, 배포 준비 상태를 확인한 뒤 결과를 보고한다.

## 작업 루틴

### 1. 요구사항 정리

- 사용자가 원하는 기능 또는 수정 내용을 요약한다.
- 구현 범위와 제외 범위를 구분한다.
- 불명확하거나 위험한 작업은 먼저 확인한다.

### 2. 코드 구조 확인

- 레포지토리 구조를 확인한다.
- 관련 파일과 진입점을 찾는다.
- 기존 구현 방식, 네이밍, 데이터 흐름을 파악한다.

### 3. 구현 계획 수립

- 수정할 파일을 식별한다.
- 구현 순서를 정한다.
- 데이터 검증이 필요한 지점을 미리 정한다.
- 위험 요소와 가정을 기록한다.
- 작업이 복잡하거나 검증 범위가 넓으면 AI 플랜, 코드 리뷰, 데이터 검증, 보조 에이전트 호출을 사용해 작업 품질을 높인다.

### 4. 코드 작성

- 필요한 최소 범위에서 코드를 수정한다.
- 중복 로직을 만들기보다 기존 유틸리티나 패턴을 재사용한다.
- 타입 안정성과 에러 처리를 유지한다.
- 임시 코드, 불필요한 로그, 하드코딩된 비밀값은 남기지 않는다.
- 고정 문자열, 숫자, URL, API 경로, 시간값, 임계값은 필요한 경우 명명된 상수나 설정으로 분리한다.
- 하드코딩이 불가피하면 이유와 영향 범위를 명확히 하고, 이후 설정화할 수 있게 구조를 단순하게 유지한다.

### 5. 데이터 검증

데이터를 다루는 기능은 다음 항목을 확인한다.

- 필수 입력값이 누락됐을 때의 동작
- 잘못된 형식의 입력값 처리
- 정상 입력값의 변환 결과
- 빈 데이터, 중복 데이터, 경계값 처리
- 저장 또는 전송되는 데이터 구조
- 화면 또는 API 응답에 표시되는 최종 결과

### 6. 무결성 검증

구현이 끝나면 다음 항목을 확인한다.

- 기존 기능이 깨지지 않았는지
- 변경된 데이터 흐름이 일관적인지
- 계산 결과가 의도한 규칙과 맞는지
- 저장된 데이터와 화면 표시 데이터가 일치하는지
- 에러 상황이 조용히 무시되지 않는지
- 불필요한 파일 변경이 섞이지 않았는지
- 하드코딩된 값이 남아 있지 않은지
- 설정, 상수, 환경 변수로 분리해야 할 값이 코드에 직접 박혀 있지 않은지

### 7. 실행 검증

프로젝트에 존재하는 검증 명령만 사용한다.

- 단위 테스트가 있으면 관련 테스트를 실행한다.
- 빌드 스크립트가 있으면 빌드를 실행한다.
- 린트 또는 타입 체크가 있으면 필요한 범위에서 실행한다.
- 검증 명령이 없으면 수동으로 확인한 내용과 한계를 명확히 남긴다.
- 자동 검증만으로 충분하지 않으면 AI 기반 리뷰나 추가 검증 도구를 호출해 논리 오류, 데이터 무결성 문제, 누락된 예외 처리를 확인한다.

## 사용자에게 보고할 내용

작업 완료 후에는 다음 형식으로 정리한다.

```text
완료 내용:
- 무엇을 구현했는지
- 어떤 파일을 변경했는지

데이터 검증:
- 어떤 입력 데이터를 확인했는지
- 어떤 출력 또는 저장 결과를 확인했는지

무결성 검증:
- 기존 동작과 충돌 여부
- 검증 명령 실행 결과

주의사항:
- 남은 제한사항
- 추가로 확인이 필요한 부분
```

## 완료 기준

다음 조건을 만족해야 작업을 완료로 본다.

- 요구사항이 코드에 반영됐다.
- 관련 데이터 흐름이 검증됐다.
- 가능한 테스트 또는 빌드 검증을 통과했다.
- 작업트리에 의도하지 않은 변경이 없다.
- 사용자에게 변경 내용과 검증 결과가 정리되어 전달됐다.
