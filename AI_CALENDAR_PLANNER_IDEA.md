# AI Calendar Planner Idea

## 앱 컨셉

사용자의 캘린더 일정, 해야 할 일, 예상 소요 시간, 마감 일정, 과거 실행 기록에서 인식한 식사 패턴과 평소 비워두는 시간대를 기반으로 AI가 하루 단위의 현실적인 계획을 자동 생성하는 개인 생산성 향상 웹앱이다.

사용자는 AI가 생성한 계획을 수행한 뒤 완료 퍼센트를 기록하고, AI는 실행 결과를 리뷰해서 다음 계획을 더 현실적으로 조정한다.

## 핵심 한 줄 설명

> 캘린더와 생활 패턴을 고려해 AI가 오늘의 현실적인 플랜을 짜고, 사용자의 실행 결과를 리뷰해 다음 계획을 개선하는 웹 기반 생산성 코치.

## 필수 조건

- 앱은 오직 웹앱으로만 개발한다.
- 모바일 네이티브 앱은 만들지 않는다.
- AI 계획 생성과 리뷰 기능은 Copilot SDK 사용을 전제로 한다.
- Copilot CLI 명령을 직접 실행하지 않고, 서버에서 Copilot SDK API로만 AI 기능을 호출한다.
- 배포 대상은 Azure 플랫폼으로 한다.
- 하드코딩은 최소화하고, 시간 규칙과 기본값은 설정 또는 상수로 분리한다.

## 주요 사용자 흐름

1. 사용자가 고정 일정을 입력한다.
2. 사용자가 해야 할 일, 중요도, 마감 일정을 입력한다.
3. 사용자가 예상 소요 시간을 알면 직접 입력하고, 잘 모르겠으면 AI에게 자연어로 설명한다.
4. 사용자가 AI 플랜 생성 전에 "오늘 너무 바빠서 이 일은 꼭 끝내야 해" 같은 추가 요청을 텍스트로 전달한다.
5. 앱이 과거 기록에서 최근 식사 패턴과 평소 일정을 잡지 않는 시간대를 인식한다.
6. 앱이 인식된 패턴을 기반으로 비가용 시간대를 계산한다.
7. AI가 사용자 요청, 마감 일정, 비가용 시간대를 반영해서 현실적인 계획을 생성한다.
8. 앱의 주 화면은 모바일 사용자가 보기 편하도록 월별 캘린더와 선택한 하루 시간표를 중심으로 보여준다.
9. AI 플랜 요청, 고정 일정, 해야 할 일, 과거 패턴, 결과, 리뷰는 별도 페이지에서 관리한다.
10. 사용자가 월별 캘린더에서 날짜를 선택하면 입력 영역 상단에서 기존 캘린더처럼 하루 시간표를 시각화한다.
11. AI 플랜 생성 후 다른 날짜를 둘러봐도 생성된 플랜은 유지되고, 하루 플랜 초기화를 눌렀을 때만 사라진다.
12. 사용자가 현재 보고 있는 날짜의 계획에 대해서만 진행률을 퍼센트로 저장한다.
13. 앱은 저장된 진행률에서 남은 작업 시간을 계산하고 다음 플랜 생성 시 미완료 분량을 반영한다.
14. AI가 진행률 결과를 리뷰하고 다음 행동을 추천한다.
15. 플랜 생성은 선택한 하루 기준으로만 수행하고, 다음날 이후 플랜은 자동으로 만들지 않는다.

## 주요 기능

### 0. 문서 액션 화면(origin/dlsdyd)

현재 앱을 덮어쓰지 않고 별도 "문서 액션" 화면으로 origin/dlsdyd 브랜치의 문서 처리 흐름을 추가한다. 이 화면은 영수증, 청구서, 계약서 텍스트를 입력받아 문서 유형을 분류하고, 지출 기록, 리마인드, 계약 확인 초안을 생성한다. 기본값은 결제 초과를 막기 위해 로컬 JSON 저장과 텍스트/파일명 기반 처리만 사용한다. 사용자가 보유 크레딧 안에서 Azure 사용을 명시적으로 허용할 때만 `DOCUMENT_ACTION_AZURE_ENABLED=true`로 Azure Document Intelligence OCR, Blob Storage 원본 저장, Cosmos DB 영구 저장을 켠다. 청구서의 납부기한이나 계약서의 만기일에서 리마인드가 생성되면 같은 날짜의 RoutineFit 캘린더에도 자동으로 일정이 추가된다.

### 1. 캘린더 기반 일정 입력

사용자가 회의, 수업, 약속처럼 이미 정해진 시간을 입력한다.

예시:

```text
10:00-11:00 회의
15:00-16:00 수업
```

이 시간대는 AI가 할 일을 배치하지 못하는 고정 일정으로 처리한다.

### 2. 해야 할 일 입력

사용자는 할 일마다 중요도와 마감 일정을 입력한다. 예상 소요 시간을 알고 있으면 직접 입력을 선택하고, 잘 모르겠으면 AI 추정을 선택해 사용자의 설명을 바탕으로 대략 계산하게 한다.

예시:

```text
발표자료 만들기 / 90분 / 중요도 높음 / 내일 발표 전까지
운동 / 40분 / 중요도 중간
메일 정리 / 20분 / 중요도 낮음
```

예상 소요 시간을 잘 모르는 경우:

```text
발표자료 만들기 / 중요도 높음 / 내일 발표 전까지
시간 입력 방식: AI 추정 선택
설명: 자료 조사는 조금 했고, 슬라이드는 아직 시작하지 못했어.
AI 예상: 120~150분
```

AI가 시간을 추정할 때는 추정 근거를 함께 보여준다.

### 3. AI 플랜 요청 텍스트 입력

AI 플랜을 만들기 전에 사용자가 현재 상황이나 강한 요구사항을 자연어로 전달할 수 있다.

예시:

```text
오늘 너무 바빠서 메일 정리는 짧게만 하고, 발표자료는 무조건 오늘 초안까지 끝내야 해.
운동은 시간이 부족하면 내일로 미뤄도 돼.
```

이 텍스트는 AI 플랜 생성 시 우선순위, 분할 방식, 오늘 처리할 범위, 다음 날로 넘길 항목을 결정하는 데 사용한다.

### 4. 과거 기록 기반 식사 패턴 인식

앱은 사용자의 과거 실행 기록, 일정 기록, 완료 리뷰 데이터를 기반으로 최근 식사 패턴을 인식한다.

사용자가 매번 식사 시간을 직접 입력하는 방식이 아니라, 기록된 생활 이벤트에서 식사 관련 시간을 추출하고 평균 식사 시간을 계산한다.

예시:

```json
{
  "lunch": ["12:20", "12:35", "12:10", "12:25"],
  "dinner": ["18:40", "19:00", "18:50"]
}
```

계산 결과:

```text
점심 평균: 12:22
저녁 평균: 18:50
```

앱은 평균 시간을 기준으로 앞뒤 여유 시간을 포함한 식사 제외 구간을 만든다.

```text
점심 제외 시간: 11:52-12:52
저녁 제외 시간: 18:20-19:20
```

### 5. 평소 일정을 잡지 않는 시간대 패턴 인식

앱은 과거 일정과 실행 기록을 분석해서 사용자가 평소 일정을 잡지 않는 시간대를 인식한다.

예시:

```text
08:00-09:00 아침 준비
22:00-23:00 잠들기 전 휴식
```

반복적으로 비어 있거나, 사용자가 자주 미완료 처리한 시간대는 비가용 후보로 판단한다.

이 시간대는 식사 패턴 시간, 고정 일정과 함께 비가용 시간대로 병합된다.

### 6. AI 자동 플랜 생성

AI는 다음 조건을 고려해서 계획을 생성한다.

- 플랜은 선택한 하루 기준으로 생성할 것
- 다음날 이후 플랜까지 자동으로 생성하지 않을 것
- 사용자가 플랜 요청에서 다르게 지시하지 않았다면 마감일까지의 고정 일정, 마감 일정, 남은 시간 압박을 함께 고려해 선택한 하루의 우선순위를 정할 것
- 고정 일정과 겹치지 않을 것
- 최근 식사 패턴 시간과 겹치지 않을 것
- 과거 패턴으로 인식된 평소 일정을 잡지 않는 시간대와 겹치지 않을 것
- 할 일의 예상 소요 시간을 지킬 것
- 예상 소요 시간이 없거나 불확실한 할 일은 AI가 설명을 바탕으로 시간을 추정할 것
- AI가 추정한 시간은 추정값임을 표시하고 근거를 함께 제공할 것
- 플랜 생성 전 사용자가 입력한 자연어 요청을 반영할 것
- 중요도와 마감 일정을 고려할 것
- 모든 할 일을 하루 안에 억지로 배치하지 않을 것
- 오늘 처리하기 어려운 일은 다음 날 이후로 이월하거나 분할할 것
- 마감 일정이 있는 할 일은 마감 기간 안에 반드시 완료되도록 계획할 것
- 여러 날에 걸친 할 일은 마감 전 완료를 보장하도록 필요한 작업 블록을 분산 배치할 것
- 긴 작업은 여러 블록으로 나눌 수 있을 것
- 작업 사이에는 적절한 휴식 시간을 둘 것

예시 출력:

```text
09:00-09:50 발표자료 만들기 1차
10:00-11:00 회의
11:10-11:30 메일 정리
11:52-12:52 점심식사
13:10-13:50 발표자료 만들기 2차
15:00-16:00 수업
16:20-17:00 발표자료 마무리
18:20-19:20 저녁식사
19:40-20:20 운동
```

### 7. 월별 캘린더와 하루 시간표 시각화

앱의 주 화면은 모바일 사용자가 바로 이해할 수 있도록 선택 날짜 요약, 월별 일정, 선택한 하루 시간표를 중심으로 보여준다. 선택 날짜 요약은 고정 일정 수, 오늘 마감 할 일 수, 플랜 생성 상태, 검증 상태를 카드로 압축해 보여준다. 기존 캘린더처럼 월별 화면에서 날짜별 고정 일정, 마감 할 일, 생성된 플랜 개수를 먼저 보여주고, 사용자가 날짜를 선택하면 선택한 하루의 시간표로 들어간다. AI 플랜 요청, 고정 일정, 해야 할 일, 과거 패턴, 결과, 리뷰는 별도 페이지로 나눠 주 화면의 복잡도를 줄인다. 생성된 플랜은 카드 목록만으로 보여주지 않고 스크롤 고정 없이 화면 상단에서 시간대별 세로 타임라인으로 보여준다. AI 플랜 생성 후 다른 날짜를 둘러봐도 플랜 데이터는 유지되며, 하루 시간표의 초기화 버튼을 눌렀을 때만 생성된 플랜이 사라진다.

검증 결과는 긴 문장 목록으로만 보여주지 않고 상태 배지, 이슈 개수, 요약 카드, 이슈 목록, AI 요약 영역으로 나눠 사용자가 한눈에 문제 여부를 판단할 수 있게 한다. 결과 화면의 생성된 플랜은 현재 설정된 날짜에 해당하는 항목만 보여주고, 다른 날짜 항목은 월별 캘린더나 해당 날짜로 이동했을 때 확인한다.

시각화에 포함할 항목:

```text
08:00 아침 준비
09:00 발표자료 만들기 1차
10:00 회의
12:00 점심식사
15:00 수업
```

고정 일정, 식사 패턴, 평소 비워두는 시간, AI가 생성한 작업 블록은 서로 다른 색상으로 구분한다. 타임라인에 들어가는 이름은 "점심식사", "저녁식사", "아침 준비", "운동"처럼 짧고 정확한 명칭으로 보여주고, 상세 근거에는 "점심식사는 보통 11:50에서 13:50 사이", "저녁식사는 보통 18:10에서 20:10 사이", "아침 준비는 보통 08:00에서 09:00 사이"처럼 현재 플랜에서 이해하기 쉬운 표현을 함께 보여준다.

### 8. 진행률 퍼센트 기록

사용자는 AI가 만든 플랜마다 완료/미완료 체크 대신 진행률을 퍼센트로 저장한다. 진행률 수정은 현재 보고 있는 날짜의 플랜에만 허용하고, 다른 날짜의 플랜은 조회만 가능하게 한다. 식사와 루틴은 과거 패턴으로 저장하고, 생성된 플랜의 진행률과 남은 시간은 이전 플랜 기록으로 따로 저장한다. 100% 미만으로 남은 작업은 다음 플랜 생성 때 이전 플랜 기록을 참고해 남은 시간으로 다시 반영된다.

진행률 예시:

```text
0%
25%
50%
75%
100%
```

진행률이 100%가 아니면 이유를 선택하거나 직접 입력할 수 있다.

예시:

```text
시간 부족
집중 안 됨
예상보다 오래 걸림
갑작스러운 일정 발생
컨디션 저하
```

### 9. 남은 작업 반영

앱은 이전 플랜 기록에서 작업별 완료 시간을 계산한다. 예를 들어 120분짜리 작업을 50%만 진행했다면 다음 플랜에서는 남은 60분만 다시 배치한다. 과거 패턴은 점심식사, 저녁식사, 아침 준비처럼 보통 비워두는 시간 계산에 사용하고, 이전 플랜 기록은 같은 작업의 진행률과 미완료 분량 판단에 사용한다.

```json
{
  "taskId": "task-1",
  "planItemId": "plan-1",
  "progressPercent": 50,
  "remainingMinutes": 60
}
```

### 10. AI 실행 리뷰

AI는 사용자의 진행률 기록과 남은 작업 시간을 기반으로 하루를 리뷰한다.

리뷰 예시:

```text
오늘 계획 전체 진행률은 68%입니다.
발표자료는 50% 진행됐고 예상보다 오래 걸렸으므로, 다음 플랜에서는 남은 작업을 40분 단위로 나누는 것이 좋습니다.
운동은 0%였지만 마감 일정이 여유 있으므로 내일 저녁보다 오후 짧은 세션으로 조정하는 것을 추천합니다.
```

## 데이터 구조 예시

### 고정 일정

```json
{
  "id": "event-1",
  "title": "회의",
  "start": "10:00",
  "end": "11:00"
}
```

### 할 일

```json
{
  "id": "task-1",
  "title": "발표자료 만들기",
  "durationMinutes": 120,
  "durationSource": "ai-estimated",
  "durationReason": "자료 조사는 일부 완료됐지만 슬라이드 제작이 남아 있어 120분으로 추정",
  "priority": "high",
  "dueSchedule": "내일 발표 전"
}
```

### 플랜 생성 전 사용자 요청

```json
{
  "planningNote": "오늘 너무 바빠서 발표자료 초안은 꼭 끝내고, 운동은 시간이 부족하면 내일로 넘겨도 돼."
}
```

### 비가용 시간대

```json
{
  "id": "blocked-1",
  "label": "점심식사",
  "reason": "점심식사는 보통 11:50에서 13:50 사이",
  "start": "11:52",
  "end": "12:52"
}
```

### 생성된 플랜

```json
{
  "id": "plan-1",
  "taskId": "task-1",
  "title": "발표자료 만들기 1차",
  "start": "09:00",
  "end": "09:50",
  "progressPercent": 0
}
```

### 실행 리뷰 입력

```json
{
  "planId": "plan-1",
  "progressPercent": 50,
  "reason": "예상보다 오래 걸림",
  "note": "자료 조사가 더 필요했음"
}
```

### 이전 플랜 기록

```json
{
  "id": "history-plan-1",
  "type": "plan",
  "taskId": "task-1",
  "planItemId": "plan-1",
  "label": "발표자료 만들기 1차",
  "date": "2026-06-20",
  "start": "09:00",
  "end": "10:00",
  "progressPercent": 50,
  "remainingMinutes": 30,
  "status": "partial"
}
```

## AI 프롬프트 방향

### 플랜 생성 프롬프트

```text
사용자의 고정 일정, 해야 할 일, 예상 소요 시간, 중요도, 마감 일정, 비가용 시간대, 이전 플랜 기록, 플랜 생성 전 사용자 요청을 기반으로 계획을 생성하세요.

플랜은 선택한 하루 기준으로만 생성하세요.
다음날 이후 플랜까지 자동으로 생성하지 마세요.
다음 시간대에는 절대 작업을 배치하지 마세요.
- 고정 일정
- 최근 식사 기록으로 계산된 식사 패턴 시간
- 과거 기록에서 인식된 평소 일정을 잡지 않는 시간대

작업은 중요도와 마감 일정을 고려해 배치하세요.
이전 플랜 기록이 있으면 같은 작업의 진행률, 남은 시간, 자주 미완료된 시간대를 참고하세요.
예상 소요 시간이 없거나 불확실한 작업은 사용자의 설명을 바탕으로 대략적인 시간을 추정하세요.
추정한 시간은 추정값임을 표시하고, 짧은 근거를 함께 제공하세요.
사용자가 "오늘 꼭 끝내야 한다", "시간이 부족하면 미뤄도 된다" 같은 요청을 남긴 경우 플랜에 반영하세요.
모든 할 일을 하루 안에 넣을 필요는 없습니다.
오늘 배치하기 어렵거나 비현실적인 작업은 다음 날 이후로 이월하거나 여러 날에 나누어 배치하세요.
마감 일정이 있는 작업은 마감 기간 안에 반드시 완료되도록 계획하세요.
여러 날에 걸친 작업은 마감 전 완료를 보장하도록 필요한 작업 블록을 분산 배치하세요.
긴 작업은 여러 작업 블록으로 나눌 수 있습니다.
각 작업 사이에는 휴식 시간을 포함하세요.
결과는 시간순 배열로 반환하세요.
```

서버에서 Copilot SDK에 전달하는 설명 프롬프트는 다음 컨텍스트를 구조화해서 포함한다.

- 선택 날짜와 사용자 플랜 요청
- 고정 일정, 비가용 시간, 해야 할 일, 검증된 플랜
- 마감일까지의 고정 일정과 작업별 필요 시간
- 검증 이슈와 AI 추정 시간 근거
- 출력 형식: 오늘의 핵심 계획, 우선순위 근거, 비가용 시간 회피, 남은 작업 또는 주의점, 바로 다음 행동

### 리뷰 프롬프트

```text
사용자의 오늘 계획, 진행률 퍼센트, 남은 작업 시간을 바탕으로 하루 실행 결과를 리뷰하세요.

리뷰에는 다음 내용을 포함하세요.
- 전체 진행률 요약
- 100% 미만으로 끝난 일의 패턴
- 다음 플랜에 반영할 남은 작업 시간
- 예상 소요 시간 조정 제안
- 내일 계획을 더 현실적으로 만들기 위한 추천
- 사용자를 격려하는 짧은 메시지
```

리뷰 프롬프트는 진행률, 남은 작업, 검증 결과, 로컬 deterministic 리뷰를 함께 전달한다. AI는 실패를 단정하지 않고 다음 플랜 조정 데이터로 해석하며, 제공된 데이터에 없는 원인이나 개인정보를 추측하지 않는다.

## Azure 운영 설계

운영 환경은 Azure Web App 배포를 기준으로 한다.

- React 클라이언트를 빌드해서 Express 서버의 `server/public` 정적 파일로 제공한다.
- `COPILOT_MODEL`, `COPILOT_TIMEOUT_MS`, `COPILOT_RUNTIME_PATH` 같은 런타임 설정은 Azure App Settings로 관리한다.
- `/api/health`에서 앱 상태, Copilot SDK 설정 준비 여부, Azure App Service 실행 여부를 비밀값 없이 확인한다.
- 배포 슬롯을 사용할 경우 staging 슬롯에서 `/api/health`와 주요 API를 확인한 뒤 production으로 교체한다.
- Azure AI Foundry 또는 Azure AI 모델 추론 엔드포인트로 확장할 때도 검증된 스케줄 컨텍스트와 구조화된 응답 계약은 유지한다.

## MVP 범위

4시간 안에 구현할 경우 다음 범위를 우선한다.

1. 월별 캘린더 개요와 기존 캘린더처럼 보이는 하루 타임라인 UI
2. 고정 일정 입력
3. 할 일, 중요도, 마감 일정 입력
4. 예상 소요 시간 직접 입력 또는 AI 추정 선택
5. 플랜 생성 전 AI에게 전달할 자연어 요청 입력
6. 과거 기록 기반 최근 식사 패턴 인식
7. 과거 기록 기반 평소 비워두는 시간대 인식
8. 인식된 패턴 기반 비가용 시간대 계산
9. Copilot SDK 기반 하루 단위 AI 플랜 생성
10. 진행률 퍼센트 기록
11. Copilot SDK 기반 AI 리뷰 생성
12. Azure 배포 준비

## 제외하거나 후순위로 둘 기능

- Google Calendar 실시간 연동
- Outlook Calendar 실시간 연동
- 모바일 네이티브 앱 개발
- 자동 휴대폰 사용 시간 수집
- 복잡한 반복 일정 규칙
- 다중 사용자 계정 시스템

## 데모 시나리오

1. 사용자가 회의와 수업을 고정 일정으로 입력한다.
2. 발표자료 만들기, 앱 개선 아이디어 조사, 운동, 메일 정리를 할 일로 입력한다.
3. 예상 시간이 애매한 발표자료와 앱 개선 아이디어 조사는 AI 추정을 선택하고 설명을 입력해서 시간을 추정하게 한다.
4. "오늘 바빠도 발표자료 초안은 꼭 끝내야 한다"는 요청을 AI에게 전달한다.
5. 앱이 과거 기록에서 최근 점심과 저녁 패턴을 인식한다.
6. 앱이 과거 기록에서 아침 준비와 잠들기 전 휴식처럼 평소 비워두는 시간대를 인식한다.
7. AI가 사용자 요청, 식사 시간, 비가용 시간대를 반영해서 플랜을 생성한다.
8. 앱이 월별 캘린더에서 날짜별 일정과 할 일 개요를 보여준다.
9. 사용자가 월별 캘린더에서 하루를 선택하면 생성된 플랜을 상단 시간대별 캘린더 화면으로 보여준다.
10. 사용자가 다른 날짜를 둘러봐도 생성된 플랜은 유지되고, 하루 플랜 초기화를 누르면 플랜이 사라진다.
11. 사용자가 각 계획의 진행률을 퍼센트로 기록한다.
12. AI가 오늘 실행 결과를 리뷰하고 내일 계획 개선안을 제안한다.
13. 다음날 플랜은 사용자가 다음날 기준으로 다시 생성할 때만 만든다.

## 기대 효과

- 사용자가 직접 하루 계획을 짜는 시간을 줄인다.
- 식사 시간과 생활 패턴을 고려해 현실적인 계획을 만든다.
- 진행률 퍼센트를 기반으로 계획 품질을 계속 개선한다.
- 미완료를 단순 실패로 보지 않고 다음 계획 개선 데이터로 활용한다.
- 개인 생산성 향상이라는 주제에 맞는 AI 코칭 경험을 제공한다.
