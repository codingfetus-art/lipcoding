# AI Calendar Planner Idea

## 앱 컨셉

사용자의 캘린더 일정, 해야 할 일, 예상 소요 시간, 마감 일정, 과거 실행 기록에서 인식한 식사 패턴과 평소 비워두는 시간대를 기반으로 AI가 현실적인 계획을 자동 생성하는 개인 생산성 향상 웹앱이다.

사용자는 AI가 생성한 계획을 수행한 뒤 완료 여부를 기록하고, AI는 실행 결과를 리뷰해서 다음 계획을 더 현실적으로 조정한다.

## 핵심 한 줄 설명

> 캘린더와 생활 패턴을 고려해 AI가 현실적인 플랜을 짜고, 사용자의 실행 결과를 리뷰해 다음 계획을 개선하는 웹 기반 생산성 코치.

## 필수 조건

- 앱은 오직 웹앱으로만 개발한다.
- 모바일 네이티브 앱은 만들지 않는다.
- AI 계획 생성과 리뷰 기능은 Copilot SDK 사용을 전제로 한다.
- 배포 대상은 Azure 플랫폼으로 한다.
- 하드코딩은 최소화하고, 시간 규칙과 기본값은 설정 또는 상수로 분리한다.

## 주요 사용자 흐름

1. 사용자가 고정 일정을 입력한다.
2. 사용자가 해야 할 일과 예상 소요 시간을 입력한다.
3. 앱이 과거 기록에서 최근 식사 패턴과 평소 일정을 잡지 않는 시간대를 인식한다.
4. 앱이 인식된 패턴을 기반으로 비가용 시간대를 계산한다.
5. AI가 비가용 시간대를 피해서 현실적인 계획을 생성한다.
6. 사용자가 각 계획을 완료했는지 기록한다.
7. AI가 완료/미완료 결과를 리뷰하고 다음 행동을 추천한다.

## 주요 기능

### 1. 캘린더 기반 일정 입력

사용자가 회의, 수업, 약속처럼 이미 정해진 시간을 입력한다.

예시:

```text
10:00-11:00 회의
15:00-16:00 수업
```

이 시간대는 AI가 할 일을 배치하지 못하는 고정 일정으로 처리한다.

### 2. 해야 할 일 입력

사용자는 할 일마다 예상 소요 시간, 중요도, 마감 일정을 입력한다.

예시:

```text
발표자료 만들기 / 90분 / 중요도 높음 / 내일 발표 전까지
운동 / 40분 / 중요도 중간
메일 정리 / 20분 / 중요도 낮음
```

### 3. 과거 기록 기반 식사 패턴 인식

앱은 사용자의 과거 실행 기록, 일정 기록, 완료 리뷰 데이터를 기반으로 최근 식사 패턴을 인식한다.

사용자가 매번 식사 시간을 직접 입력하는 방식이 아니라, 기록된 생활 이벤트에서 식사 관련 시간을 추출하고 평균 식사 시간을 계산한다.

예시:

```json
{
  "lunch": ["12:20", "12:35", "12:10", "12:25"],
  "dinner": ["18:40", "19:00", "18:50"]
}
```

계산 결과:

```text
점심 평균: 12:22
저녁 평균: 18:50
```

앱은 평균 시간을 기준으로 앞뒤 여유 시간을 포함한 식사 제외 구간을 만든다.

```text
점심 제외 시간: 11:52-12:52
저녁 제외 시간: 18:20-19:20
```

### 4. 평소 일정을 잡지 않는 시간대 패턴 인식

앱은 과거 일정과 실행 기록을 분석해서 사용자가 평소 일정을 잡지 않는 시간대를 인식한다.

예시:

```text
08:00-09:00 아침 준비
22:00-23:00 잠들기 전 휴식
```

반복적으로 비어 있거나, 사용자가 자주 미완료 처리한 시간대는 비가용 후보로 판단한다.

이 시간대는 식사 패턴 시간, 고정 일정과 함께 비가용 시간대로 병합된다.

### 5. AI 자동 플랜 생성

AI는 다음 조건을 고려해서 계획을 생성한다.

- 고정 일정과 겹치지 않을 것
- 최근 식사 패턴 시간과 겹치지 않을 것
- 과거 패턴으로 인식된 평소 일정을 잡지 않는 시간대와 겹치지 않을 것
- 할 일의 예상 소요 시간을 지킬 것
- 중요도와 마감 일정을 고려할 것
- 모든 할 일을 하루 안에 억지로 배치하지 않을 것
- 오늘 처리하기 어려운 일은 다음 날 이후로 이월하거나 분할할 것
- 마감 일정이 있는 할 일은 마감 기간 안에 반드시 완료되도록 계획할 것
- 여러 날에 걸친 할 일은 마감 전 완료를 보장하도록 필요한 작업 블록을 분산 배치할 것
- 긴 작업은 여러 블록으로 나눌 수 있을 것
- 작업 사이에는 적절한 휴식 시간을 둘 것

예시 출력:

```text
09:00-09:50 발표자료 만들기 1차
10:00-11:00 회의
11:10-11:30 메일 정리
11:52-12:52 점심 패턴 시간
13:10-13:50 발표자료 만들기 2차
15:00-16:00 수업
16:20-17:00 발표자료 마무리
18:20-19:20 저녁 패턴 시간
19:40-20:20 운동
```

### 6. 완료 여부 기록

사용자는 AI가 만든 플랜마다 완료 여부를 기록한다.

상태 예시:

```text
완료
미완료
부분 완료
건너뜀
```

미완료 또는 부분 완료인 경우 이유를 선택하거나 직접 입력할 수 있다.

예시:

```text
시간 부족
집중 안 됨
예상보다 오래 걸림
갑작스러운 일정 발생
컨디션 저하
```

### 7. AI 실행 리뷰

AI는 사용자의 완료 기록을 기반으로 하루를 리뷰한다.

리뷰 예시:

```text
오늘은 5개 계획 중 3개를 완료했습니다.
발표자료 작업은 예상보다 오래 걸렸기 때문에 다음에는 90분 작업을 3개의 작은 블록으로 나누는 것이 좋습니다.
저녁 이후 운동은 완료율이 낮으므로 내일은 오후 16시 이후의 짧은 산책으로 조정하는 것을 추천합니다.
```

## 데이터 구조 예시

### 고정 일정

```json
{
  "id": "event-1",
  "title": "회의",
  "start": "10:00",
  "end": "11:00"
}
```

### 할 일

```json
{
  "id": "task-1",
  "title": "발표자료 만들기",
  "durationMinutes": 90,
  "priority": "high",
  "dueSchedule": "내일 발표 전"
}
```

### 비가용 시간대

```json
{
  "id": "blocked-1",
  "reason": "점심 패턴",
  "start": "11:52",
  "end": "12:52"
}
```

### 생성된 플랜

```json
{
  "id": "plan-1",
  "taskId": "task-1",
  "title": "발표자료 만들기 1차",
  "start": "09:00",
  "end": "09:50",
  "status": "pending"
}
```

### 실행 리뷰 입력

```json
{
  "planId": "plan-1",
  "status": "partial",
  "reason": "예상보다 오래 걸림",
  "note": "자료 조사가 더 필요했음"
}
```

## AI 프롬프트 방향

### 플랜 생성 프롬프트

```text
사용자의 고정 일정, 해야 할 일, 예상 소요 시간, 중요도, 마감 일정, 비가용 시간대를 기반으로 계획을 생성하세요.

다음 시간대에는 절대 작업을 배치하지 마세요.
- 고정 일정
- 최근 식사 기록으로 계산된 식사 패턴 시간
- 과거 기록에서 인식된 평소 일정을 잡지 않는 시간대

작업은 중요도와 마감 일정을 고려해 배치하세요.
모든 할 일을 하루 안에 넣을 필요는 없습니다.
오늘 배치하기 어렵거나 비현실적인 작업은 다음 날 이후로 이월하거나 여러 날에 나누어 배치하세요.
마감 일정이 있는 작업은 마감 기간 안에 반드시 완료되도록 계획하세요.
여러 날에 걸친 작업은 마감 전 완료를 보장하도록 필요한 작업 블록을 분산 배치하세요.
긴 작업은 여러 작업 블록으로 나눌 수 있습니다.
각 작업 사이에는 휴식 시간을 포함하세요.
결과는 시간순 배열로 반환하세요.
```

### 리뷰 프롬프트

```text
사용자의 오늘 계획과 완료 여부를 바탕으로 하루 실행 결과를 리뷰하세요.

리뷰에는 다음 내용을 포함하세요.
- 완료한 일 요약
- 미완료 또는 부분 완료된 일의 패턴
- 예상 소요 시간 조정 제안
- 내일 계획을 더 현실적으로 만들기 위한 추천
- 사용자를 격려하는 짧은 메시지
```

## MVP 범위

4시간 안에 구현할 경우 다음 범위를 우선한다.

1. 내부 캘린더 또는 하루 타임라인 UI
2. 고정 일정 입력
3. 할 일과 예상 소요 시간 입력
4. 과거 기록 기반 최근 식사 패턴 인식
5. 과거 기록 기반 평소 비워두는 시간대 인식
6. 인식된 패턴 기반 비가용 시간대 계산
7. Copilot SDK 기반 AI 플랜 생성
8. 완료 여부 기록
9. Copilot SDK 기반 AI 리뷰 생성
10. Azure 배포 준비

## 제외하거나 후순위로 둘 기능

- Google Calendar 실시간 연동
- Outlook Calendar 실시간 연동
- 모바일 네이티브 앱 개발
- 자동 휴대폰 사용 시간 수집
- 복잡한 반복 일정 규칙
- 다중 사용자 계정 시스템

## 데모 시나리오

1. 사용자가 회의와 수업을 고정 일정으로 입력한다.
2. 발표자료 만들기, 운동, 메일 정리를 할 일로 입력한다.
3. 앱이 과거 기록에서 최근 점심과 저녁 패턴을 인식한다.
4. 앱이 과거 기록에서 아침 준비와 잠들기 전 휴식처럼 평소 비워두는 시간대를 인식한다.
5. AI가 인식된 식사 시간과 비가용 시간대를 피해서 하루 플랜을 생성한다.
6. 사용자가 일부 계획을 완료하고 일부는 미완료로 표시한다.
7. AI가 오늘 실행 결과를 리뷰하고 내일 계획 개선안을 제안한다.

## 기대 효과

- 사용자가 직접 하루 계획을 짜는 시간을 줄인다.
- 식사 시간과 생활 패턴을 고려해 현실적인 계획을 만든다.
- 완료 여부를 기반으로 계획 품질을 계속 개선한다.
- 미완료를 단순 실패로 보지 않고 다음 계획 개선 데이터로 활용한다.
- 개인 생산성 향상이라는 주제에 맞는 AI 코칭 경험을 제공한다.
