# Judging Criteria

이 문서는 RoutineFit AI Scheduler를 심사 기준에 맞춰 점검하기 위한 평가 기준과 프로젝트 대응 체크리스트를 정리한다.

## 핵심 평가 방향

기능 개수보다 앱의 핵심 문제를 얼마나 깊게 해결하는지, AI가 실제 가치의 중심에 있는지, 클라우드와 사용자 경험이 자연스럽게 연결되는지를 우선 평가한다.

## 가중치 요약

| 항목 | 평가 기준 | 가중치 |
| --- | --- | ---: |
| 1 | Copilot SDK와 AI 핵심성 | 25% |
| 2 | 대상 사용자와 실질적 생산성 이점 | 18% |
| 3 | Azure 클라우드 활용도 | 18% |
| 4 | 엔드투엔드 완성도와 구현 품질 | 16% |
| 5 | 자연스러운 AI 통합과 사용자 경험 | 12% |
| 6 | 책임 있는 AI와 보안 | 6% |
| 7 | 창의성과 차별성 | 5% |
| 합계 |  | 100% |

## 1. Copilot SDK와 AI 핵심성 - 25%

Copilot SDK가 앱 가치의 핵심적인 역할을 하는지 평가한다.

평가 포인트:

- AI가 단순 부가 기능이 아니라 하루 계획 생성, 실행 리뷰, 다음 계획 개선의 중심 역할을 하는가
- 프롬프트 설계가 고정 일정, 해야 할 일, 진행률, 과거 패턴 같은 충분한 컨텍스트를 반영하는가
- 에이전트 설계, 도구 호출, 컨텍스트 처리, 스트리밍 또는 응답 처리 품질이 적절한가
- AI 기능의 개수보다 하나의 생산성 문제를 깊고 일관되게 해결하는가

RoutineFit 체크리스트:

- 서버에서 Copilot SDK를 직접 호출하고 Copilot CLI 명령에 의존하지 않는다.
- AI 플랜 생성과 AI 실행 리뷰가 앱의 핵심 사용자 흐름에 포함된다.
- 선택 날짜, 사용자 요청, 고정 일정, 비가용 시간, 검증 결과, 진행률, 남은 작업을 구조화된 컨텍스트로 전달한다.
- AI 응답은 핵심 계획, 우선순위 근거, 비가용 시간 회피, 남은 작업, 다음 행동 같은 고정 섹션으로 요청한다.
- AI 실패 시 로컬 검증 플랜과 명시적 오류를 보여줘 실패가 조용히 숨겨지지 않는다.

## 2. 대상 사용자와 실질적 생산성 이점 - 18%

명확한 대상 사용자에게 현실적이고 입증 가능한 생산성 이점을 제공하는지 평가한다.

평가 포인트:

- 대상 사용자가 명확한가
- 사용자가 실제로 겪는 계획 수립, 시간 배치, 미완료 관리 문제를 해결하는가
- 해결 방식이 과장되지 않고 현실적인 제약을 반영하는가
- 데모나 데이터 흐름으로 이점이 입증 가능한가

RoutineFit 체크리스트:

- 개인 생산성 향상이 필요한 사용자를 대상으로 한다.
- 캘린더 일정, 마감, 식사 패턴, 평소 비워두는 시간, 진행률을 함께 고려한다.
- 미완료를 실패로 끝내지 않고 다음 플랜의 남은 작업으로 반영한다.

## 3. Azure 클라우드 활용도 - 18%

실제로 의미 있게 Azure 클라우드를 활용하는지 평가한다.

평가 포인트:

- 단순히 Azure 서비스를 끼워 넣는 수준이 아니라 앱 구조와 운영에 클라우드가 의미 있게 연결되는가
- AI 모델 계층은 Microsoft Azure AI Foundry 또는 Azure AI 계층에서 동작할 수 있도록 설계되어 있는가
- 클라우드 네이티브 프랙티스와 관련성이 있는가
- 배포, 설정, 비밀 정보, 환경 변수, 관측 가능성 같은 운영 요소를 고려하는가

RoutineFit 체크리스트:

- Azure Web App 배포 흐름을 준비한다.
- AI 런타임, 모델, 타임아웃 등은 환경 변수로 분리한다.
- Azure App Settings로 `COPILOT_MODEL`, `COPILOT_TIMEOUT_MS`, `COPILOT_RUNTIME_PATH`를 관리할 수 있다.
- `/api/health`로 App Service 상태와 Copilot SDK 설정 준비 여부를 비밀값 없이 확인할 수 있다.
- staging deployment slot에서 헬스 체크와 API smoke test 후 production swap을 수행할 수 있다.
- 향후 Azure AI Foundry 또는 Azure AI 계층으로 모델 실행 경로를 확장할 수 있어야 한다.

감점 주의:

- 앱 가치와 무관한 Azure 서비스 추가
- 클라우드 리소스를 사용하지만 로컬 앱과 실질적 차이가 없는 구성
- 비밀값을 코드에 직접 넣는 방식

## 4. 엔드투엔드 완성도와 구현 품질 - 16%

애플리케이션이 엔드투엔드로 적당히 완성되어 있고 플랫폼에 맞게 구현됐는지 평가한다.

평가 포인트:

- 프론트엔드, 백엔드, AI 호출, 검증, 오류 처리가 연결되어 있는가
- 코드 품질과 아키텍처가 유지보수 가능한가
- 입력 검증, 에러 처리, 성능, 플랫폼 적합성이 적절한가
- 네이티브, 크로스 플랫폼, 반응형 웹 등 선택한 플랫폼에 맞는 UX를 제공하는가

RoutineFit 체크리스트:

- React 웹앱과 Express 서버가 API로 연결된다.
- 월별 일정, 하루 시간표, 플랜 설정, 결과 리뷰가 사용자 흐름으로 이어진다.
- 모바일 사용자를 위해 주 화면을 월별 일정과 하루 플랜 중심으로 정리한다.
- 서버 테스트와 클라이언트 빌드로 기본 동작을 검증한다.

## 5. 자연스러운 AI 통합과 사용자 경험 - 12%

사용자의 저항이 낮고 자연스럽게 AI가 통합되어 있는지 평가한다.

평가 포인트:

- 사용자가 AI를 억지로 쓰는 느낌 없이 자연스럽게 도움을 받는가
- 지연, 오류, AI 실패 상태가 투명하고 우아하게 처리되는가
- 접근성과 반응형 UI가 고려되어 있는가
- 자동화가 과도하게 주도권을 가져가지 않고 사용자가 최종 통제권을 유지하는가

RoutineFit 체크리스트:

- AI 추정은 사용자가 직접 선택할 수 있다.
- 진행률 저장, 하루 플랜 초기화, 날짜 선택은 사용자가 직접 제어한다.
- AI 실패 시에도 검증된 로컬 플랜을 보여주고 `aiError`를 표시한다.
- 현재 보고 있는 날짜의 플랜만 진행률을 수정할 수 있어 실수 가능성을 줄인다.

## 6. 책임 있는 AI와 보안 - 6%

AI 출력의 투명성, 사람의 확인, 데이터 프라이버시, 환각 완화, 프롬프트 인젝션 인지, 비밀 정보 처리를 평가한다.

평가 포인트:

- AI가 만든 결과와 검증된 로컬 결과를 구분할 수 있는가
- 위험하거나 되돌리기 어려운 작업 전에 사람의 확인이 있는가
- 사용자 데이터와 비밀 정보가 안전하게 처리되는가
- 환각을 줄이기 위한 deterministic validation 또는 규칙 기반 검증이 있는가
- 프롬프트 인젝션 가능성을 인지하고 입력 컨텍스트를 제한하는가

RoutineFit 체크리스트:

- AI 결과는 서버 검증 결과를 기반으로 설명한다.
- 일정 겹침, 비가용 시간 충돌, 마감 내 배치 가능 여부를 deterministic validation으로 검증한다.
- 비밀값은 환경 변수로 분리하고 코드에 커밋하지 않는다.
- AI 실패나 불확실성을 사용자에게 명시적으로 보여준다.

## 7. 창의성과 차별성 - 5%

기존 서비스나 도구의 뻔한 복제가 아니라 AI를 생산성 측면에서 새롭고 창의적으로 적용했는지 평가한다.

평가 포인트:

- 단순 캘린더, 단순 할 일 앱, 단순 챗봇 복제에 그치지 않는가
- AI가 생산성 문제를 새 방식으로 해결하는가
- 개인 패턴, 진행률, 미완료 데이터를 다음 계획 개선에 활용하는가
- 데모에서 차별점이 명확하게 드러나는가

RoutineFit 체크리스트:

- 식사 패턴과 평소 비워두는 시간대를 자동으로 추론한다.
- 진행률 기반 남은 작업을 다음 플랜에 반영한다.
- 하루 단위 계획만 생성해 과도한 자동화를 피한다.
- 월별 보기와 하루 시간표를 결합해 사용자가 계획 맥락을 쉽게 확인한다.

## 최종 점검 체크리스트

- Copilot SDK가 앱 핵심 흐름에 실제로 사용되는가
- AI 프롬프트가 충분한 컨텍스트와 제약을 반영하는가
- 대상 사용자와 해결하는 생산성 문제가 명확한가
- Azure 배포와 Azure AI 계층 확장 가능성이 준비되어 있는가
- 모바일 반응형 웹으로 주 화면과 보조 페이지가 정리되어 있는가
- AI 실패, 오류, 지연 상태가 투명하게 드러나는가
- 진행률과 남은 작업이 다음 플랜 개선에 반영되는가
- 비밀 정보와 사용자 데이터가 안전하게 처리되는가
- 단순 복제가 아니라 RoutineFit만의 생산성 개선 흐름이 보이는가

## 현재 구현 예상 점수

내가 심사자라면 현재 구현은 약 **80점 / 100점**으로 평가할 것 같다.

| 항목 | 가중치 | 예상 점수 | 가중 반영 | 판단 |
| --- | ---: | ---: | ---: | --- |
| Copilot SDK와 AI 핵심성 | 25 | 20 | 20.0 | Copilot SDK 호출, 구조화된 컨텍스트, 고정 응답 섹션, 로컬 검증 fallback은 강점이다. 스트리밍과 실제 도구 호출 설계는 아직 제한적이다. |
| 대상 사용자와 생산성 이점 | 18 | 15 | 15.0 | 개인 생산성 문제와 미완료 이월 흐름이 명확하다. 실제 사용자 검증 데이터가 있으면 더 강해진다. |
| Azure 클라우드 활용도 | 18 | 13 | 13.0 | Azure Web App 배포, App Settings 기반 런타임 설정, `/api/health`, deployment slot 운영 방향을 문서화했다. 실제 Azure AI Foundry 연동은 아직 확장 계획 수준이다. |
| 엔드투엔드 완성도 | 16 | 13 | 13.0 | 프론트, 서버, AI 호출, 검증, 모바일 UI가 연결되어 있다. 영속 DB와 인증/관측성은 아직 없다. |
| 자연스러운 AI UX | 12 | 10 | 10.0 | AI 추정 선택, 오류 표시, 진행률 저장, 모바일 화면 분리는 좋다. 로딩/지연 상태 UX는 더 다듬을 수 있다. |
| 책임 있는 AI와 보안 | 6 | 5 | 5.0 | deterministic validation과 명시적 `aiError`는 강점이다. 프롬프트 인젝션 방어와 개인정보 정책 문서는 보강 여지가 있다. |
| 창의성과 차별성 | 5 | 4 | 4.0 | 식사 패턴, 비가용 시간, 진행률 기반 다음 플랜 반영은 차별점이 있다. 데모 스토리를 더 선명하게 만들면 좋아진다. |
| 합계 | 100 |  | 80.0 | 구조화된 AI 컨텍스트와 Azure 운영 설명을 반영하면 약 80점 수준을 기대할 수 있다. 실제 Azure AI Foundry 연동과 스트리밍 UX까지 보강하면 추가 상승 여지가 있다. |

### 점수를 가장 빨리 올릴 수 있는 보강 포인트

1. **Azure AI Foundry 또는 Azure AI 계층 연계 설계 보강**: 현재 가장 큰 감점 구간이다.
2. **Copilot SDK 활용 깊이 강화**: 프롬프트 컨텍스트 구조, 스트리밍 또는 단계적 AI 응답, 도구 호출 설계를 명확히 보여준다.
3. **데모 시나리오 강화**: 사용자가 미완료한 작업이 다음 플랜에서 남은 시간으로 반영되는 장면을 핵심 데모로 보여준다.
4. **책임 있는 AI 문서화**: 데이터 프라이버시, 프롬프트 인젝션 인지, AI 출력 검증 절차를 짧게라도 문서화한다.
